# Normalize line endings to LF in the repo — prevents CRLF/LF churn across
# different machines and AI sessions. Text files stored and diffed as LF.
* text=auto eol=lf

# Binary assets — never touch
*.png binary
*.jpg binary
*.jpeg binary
*.gif binary
*.pdf binary
*.docx binary
*.pptx binary
*.zip binary
