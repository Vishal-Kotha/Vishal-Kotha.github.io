# Hugo build output — regenerated by CI on every deploy, no need to track
/public/
/resources/_gen/
.hugo_build.lock

# OS / editor junk
.DS_Store
Thumbs.db
*.swp

# Debug / session artifacts
*debug-session*.zip
kimi-debug-session_*.zip
